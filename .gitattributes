# Normalize line endings to LF in the repo, let Git convert on checkout per-platform
* text=auto eol=lf

# Notebooks: treat as text for diffing, but avoid renormalizing generated output noise
*.ipynb text eol=lf

# Explicit binary types
*.png binary
*.jpg binary
*.jpeg binary
*.pdf binary
